# 📱 Telegram ⇄ 電腦端專屬 Agent 私人通道使用指南 (3 分鐘極速啟用)

> 🌟 **核心亮點**：
> - 📱 **手機專屬獨立 Agent**：手機端 Telegram 獨立為專屬 Agent 視窗，問答純淨直達，不與電腦其他對話視窗衝突！
> - 🌲 **視覺化樹狀地圖 (`/tree`)**：手機一鍵瀏覽完整專案目錄樹，點擊按鈕自由切換資料夾！
> - 📦 **100% 獨立存放隔離**：收件匣與歷史 Log 皆存放於 `Telegram_Agent_Bridge` 專案目錄下，不污染其他資料夾。
> - 🚚 **相片直達與整批搬移**：傳圖自動存入指定資料夾，支援說話整批搬移與清空暫存區！
> - ⚡ **0 Spark 額度消耗**：完全利用家中開機電腦的本地算力與 NVIDIA 旗艦模型，免費且無任何用量上限！

---

## 🚀 三步快速啟用步驟

### 步驟 1：建立你的私人專屬 Telegram Bot（耗時約 30 秒）
1. 在手機或電腦打開 Telegram，搜尋 **`@BotFather`**（官方藍勾勾帳號）。
2. 在對話框發送：`/newbot`
3. 依照提示設定機器人名稱（例如 `BryanAgentBot`），並設定一個以 `bot` 結尾的 username（例如 `shihwei_agent_bot`）。
4. BotFather 會發給你一串 **`HTTP API Token`**（格式例如 `7123456789:AAH...`）。

---

### 步驟 2：填入 Token
將取得的 Token 複製，貼入檔案：
👉 [bridge_config.json](file:///c:/Users/yexia/Documents/ShihWei/NTNU/GitHub/Telegram_Agent_Bridge/bridge_config.json)

```json
{
    "bot_token": "貼上你的_TELEGRAM_BOT_TOKEN",
    "allowed_user_id": 0,
    "workspace_root": "c:\\Users\\yexia\\Documents\\ShihWei\\NTNU\\GitHub",
    "current_project": "Telegram_Agent_Bridge",
    "auto_sync_agent_replies": false,
    "poll_interval_seconds": 1.0
}
```

---

### 步驟 3：在電腦端啟動守護進程
雙擊執行 `啟動Telegram橋接器.bat` 或在終端機執行：
```powershell
python "c:\Users\yexia\Documents\ShihWei\NTNU\GitHub\Telegram_Agent_Bridge\antigravity_telegram_bridge.py"
```

---

## 📱 手機端常用快捷操作
1. 在手機 Telegram 搜尋並點開你剛剛建立的機器人。
2. 發送 `/start` 或 `/pin`，系統會自動鎖定你的專屬 ID 並發送操作面板。
3. 常用指令一覽：
   - `/tree`：動態查看 ASCII 樹狀地圖並使用按鈕切換資料夾。
   - `/cd <目錄名>`：切換工作目錄（如 `/cd mobile` 或 `/cd ..`）。
   - `/ls`：查看當前目錄下的所有檔案。
   - `/pwd`：檢視目前所在工作專案與圖片存放目標。
   - `/staging`：查看【手機上傳臨時存放區】中的暫存檔案。
   - `/status`：查看手機專屬 Agent 的健康度與狀態。
   - `/apk`：電腦自動將最新編譯好的氣氛燈 APK 傳到手機。
   - `/clear`：重置手機即時收件匣與對話記憶。
   - 直接發送訊息、傳送圖片或指派指令，專屬 Mobile Agent 隨身為你服務！🌟
