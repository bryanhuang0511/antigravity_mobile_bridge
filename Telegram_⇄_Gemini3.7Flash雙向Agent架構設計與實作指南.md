# 🤖 Telegram ⇄ Gemini 3.7 Flash 雙向 Agent 架構設計與實作指南

> 🚀 **核心定位**：打造手機 Telegram 隨身操控 ⇄ 電腦端 Antigravity (Gemini 3.7 Flash 旗艦大腦) 深度連動的雙向智能體系統，實現「手機發令、電腦端頂級大腦編程、成果與 APK 秒傳回手機」的完全自動化閉環！

---

## 🌟 一、架構設計理念：雙層智能協同 (Dual-Layer Agentic Pipeline)

我們將整個系統拆分為兩大層級，各司其職、完美互補：

```
┌─────────────────────────────────────────────────────────────┐
│ 📱 手機端 Telegram (隨身操控介面)                             │
│   • 語音/文字交辦任務                                         │
│   • /tree 資料夾結構瀏覽與專案切換                           │
│   • 批次照片上傳與自動分類                                   │
└──────────────────────────────┬──────────────────────────────┘
                               │ (Telegram Bot API)
                               ▼
┌─────────────────────────────────────────────────────────────┐
│ 📡 Telegram Agent Bridge 守護進程 (Python Background Daemon) │
│   • 輕量任務：調用本機 Python / PowerShell 秒級自治執行      │
│   • 重度編程：結構化寫入 📱_手機Telegram即時收件匣.md        │
│   • 即時回饋：In-Place 原地變形 Working 進度提示             │
│   • 成果監聽：AgentOutboxWatcher 自動監控並推播結果與 APK    │
└──────────────────────────────┬──────────────────────────────┘
                               │ (雙向信號管線)
                               ▼
┌─────────────────────────────────────────────────────────────┐
│ 💻 電腦端 Antigravity IDE (Gemini 3.7 Flash 旗艦大腦)        │
│   • 讀取即時收件匣中的重度任務                               │
│   • 真正修改跨檔案代碼 (Java/Kotlin/Python/Gradle)           │
│   • 執行終端編譯與測試，產出全新 .apk 安裝包                 │
│   • 寫入執行成果報告至收件匣                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## ⚙️ 二、核心功能模組實作細節

### 1. 📥 結構化即時收件匣 (Structured Inbox)
當手機發送包含「寫程式」、「修改代碼」、「重構」、「加功能」、「編譯 APK」等重度任務時：
* Bridge 立即識別為 **重度編程任務**。
* Telegram 顯示：`📥 已接收任務！⚡ 正在指派給電腦端 Gemini 3.7 Flash 旗艦主腦...`。
* 將任務結構化寫入 `📱_手機Telegram即時收件匣.md`，包含：
  * 🕒 時間戳記
  * 🎯 目標專案與實體路徑
  * 💬 夥伴完整指令與補充說明
  * 🟡 狀態：`🟡 等待電腦端 Gemini 3.7 Flash 執行中...`

### 2. 📡 雙向成果監聽與自動推播器 (AgentOutboxWatcher)
Bridge 內部常駐一個輕量監聽線程：
* **監聽收件匣更新**：一旦電腦端 Gemini 3.7 Flash 完成任務，將狀態更新為 `🟢 執行完成` 並寫入結果報告時，監聽器會立即捕捉。
* **推播報告至手機**：自動將最新代碼修改重點推送到手機 Telegram。
* **自動偵測並傳送 APK**：若任務過程中生成了新的 `.apk` 安裝包，監聽器會自動調用 `sendDocument` API，把安裝包直接送到手機聊天室！

### 3. 🐳 本地零 Token 輕量工具協同 (CodeWhale Integration)
* 針對本地簡單的代碼檢視或輕量腳本執行，Bridge 內建 `/codewhale <指令>` 支援。
* 直接調用 `02_AI編程智能體_ZeroToken_CodeWhale_RooCode/CodeWhale/codewhale.exe` 在本機執行，完全 0 Token、不佔用外部額度。

---

## 🔒 三、代碼防壞與安全防禦守則

1. **Git 歷史安全網**：所有專案皆納入版本控制，隨時可一鍵 `git restore` 回滾。
2. **重度編程專屬主腦**：所有涉及跨檔案、多模組修改的代碼，嚴格交由具備長上下文推理的 **Gemini 3.7 Flash** 執行，絕不交給輕量模型盲目改寫。
3. **軟體安全最高防禦**：未經夥伴明確授權，嚴禁擅自刪除重要系統檔案。
